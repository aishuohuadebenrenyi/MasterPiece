const {
  normalizePracticeRecord,
  listPracticeRecords,
  createPracticeRecord
} = require('./practice-record')

module.exports = {
  normalizeGameRecord: normalizePracticeRecord,
  listGameRecords: listPracticeRecords,
  createGameRecord: createPracticeRecord,
  normalizePracticeRecord,
  listPracticeRecords,
  createPracticeRecord
}
