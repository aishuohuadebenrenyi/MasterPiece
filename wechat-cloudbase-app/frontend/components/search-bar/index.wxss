@import "../../styles/theme.wxss";

:host {
  display: block;
  width: 100%;
}

.search-bar {
  display: flex;
  align-items: center;
  gap: 14rpx;
  min-height: 84rpx;
  padding: 0 24rpx;
  border-radius: 28rpx;
  background: var(--improv-card-light);
  border: 2rpx solid var(--improv-line-soft);
  box-sizing: border-box;
}

.search-bar-compact {
  min-height: 76rpx;
  padding: 0 20rpx;
  border-radius: 24rpx;
}

.search-icon {
  flex: 0 0 auto;
  color: var(--improv-light);
  font-size: 26rpx;
  line-height: 1;
}

.search-input {
  flex: 1 1 auto;
  width: 0;
  min-width: 0;
  height: 84rpx;
  margin: 0;
  padding: 0;
  background: transparent;
  color: var(--improv-text);
  font-size: 28rpx;
}

.search-bar-compact .search-input {
  height: 76rpx;
  font-size: 26rpx;
}

.search-clear,
.search-hint {
  flex: 0 0 auto;
  color: var(--improv-light);
  font-size: 22rpx;
  line-height: 1.4;
}

.search-clear {
  flex: 0 0 auto;
  width: 96rpx;
  min-width: 96rpx;
  max-width: 96rpx;
  min-height: 56rpx;
  margin: 0;
  padding: 0;
  border-radius: 999rpx;
  background: var(--improv-btn-toolbar-bg);
  border: 2rpx solid var(--improv-btn-toolbar-border);
  color: var(--improv-btn-toolbar-text);
  font-size: 22rpx;
  font-weight: 800;
  line-height: 1;
  display: inline-flex;
  align-items: center;
  justify-content: center;
  white-space: nowrap;
}
